# License Review Notes

This file records manual license-review notes for packages that require human review.

This is an engineering audit aid, not legal advice.

## Current Audit Summary

Generated by:

```bash
npm run audit:licenses
```

The generated reports are local-only and should not be committed:

```txt
reports/license-inventory.json
reports/license-inventory.md
```

Current generated audit summary:

```txt
Total packages: 616
ok: 586
review: 27
notice: 2
unknown: 1
```

## Manual Review Findings

### @better-fetch/fetch@1.1.21

Status: accepted with upstream MIT evidence

Reason:

- This package appears as a transitive production dependency through `better-auth`.
- The installed package metadata currently does not include `license`, `repository`, or `homepage`.
- The upstream `better-auth/better-fetch` repository README lists the project license as MIT.
- There is an upstream issue requesting that the package add the missing `license` key to `package.json`.

Decision:

- Keep because it is required by `better-auth`.
- Upstream repository documents MIT license.
- Installed npm package metadata is missing the license field, so generated reports may still show UNKNOWN until upstream fixes the package.
- Recheck before production dependency upgrades.

### sharp / libvips optional packages

Status: accepted for hosted deployment

Reason:

- Several optional transitive packages related to Sharp/libvips are licensed under LGPL-3.0-or-later or mixed Apache-2.0/LGPL/MIT terms.
- These packages are optional transitive dependencies and should be reviewed before final client delivery.

Decision:

- These packages are optional transitive dependencies.
- The app does not directly import or modify Sharp/libvips.
- Keep package license files intact.
- Reopen review only if distributing Docker images, server binaries, or a packaged production artifact to a client.

### lightningcss and axe-core

Status: accepted

Reason:

- These packages are licensed under MPL-2.0.
- Most flagged `lightningcss` packages are optional transitive dependencies.
- `axe-core` is a development transitive dependency.

Decision:

- `lightningcss` is not a direct application dependency.
- No project code modifies `lightningcss`.
- Keep dependency license files intact.
- No replacement needed.

- `axe-core` is a development transitive dependency.
- It is not application runtime code.
- No project code modifies `axe-core`.
- Keep dependency license files intact.

### argparse

Status: accepted

Reason:

- `argparse` uses Python-2.0.

Decision:

- `argparse` is a development transitive dependency.
- No project code modifies it.
- Keep license notices intact.
- No replacement needed.

### caniuse-lite

Status: accepted with attribution note

- `caniuse-lite` uses CC-BY-4.0.

Decision:

- `caniuse-lite` is a transitive browser compatibility data package.
- Keep package license files intact.
- Include `caniuse-lite — CC-BY-4.0` in third-party/license review notes.
- No replacement needed.

## Final Commercial Delivery Reminder

Before handing the project to a real client or launching production:

- Regenerate the license report with `npm run audit:licenses`.
- Review any `unknown`, `review`, or `high-review` entries.
- Review all images, logos, icons, fonts, templates, and product media.
- Keep proof of license or permission for any commercial assets.
- Treat this file as an engineering note, not a substitute for legal advice.
